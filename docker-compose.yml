services:
  # mariadb:
  #   image: mariadb:latest
  #   container_name: mariadb_container
  #   restart: always
  #   environment:
  #     MYSQL_ROOT_PASSWORD: m@r!aR00tP@$s
  #     MYSQL_DATABASE: db_bannedBooks
  #     MYSQL_USER: svc_express
  #     MYSQL_PASSWORD: ^CdGWQ1Ly6HGySY6pcY1
  #   volumes:
  #     - mariadb_data:/var/lib/mysql
  #   ports:
  #     - "3306:3306"

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: express_backend
    restart: always
    ports:
      - "8080:8080"
    # environment:
    #   DATABASE_URL: "mysql://svc_express:^CdGWQ1Ly6HGySY6pcY1@mariadb:3306/db_bannedBooks"
    # depends_on:
    #   - mariadb

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: react_frontend
    restart: always
    ports:
      - "3000:80"
    depends_on:
      - backend

# volumes:
#   mariadb_data: